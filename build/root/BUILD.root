load("@bazel_gazelle//:def.bzl", "gazelle")

gazelle(
    name = "gazelle",
    external = "vendored",
    extra_args = [
        "-build_file_name",
        "BUILD,BUILD.bazel",
    ],
    prefix = "github.com/jetstack/navigator",
)
